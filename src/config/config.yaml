logger:
  level: 'info'
  file: 'out.log'

auth:
  signingKey: "racket_shop"
  accessTokenTTL: 100m
  refreshTokenTTL: 120h

http:
  port: 8080

database:
  postgres:
    host: 'localhost'
    port: 5432
    user: 'postgres'
    password: 'admin'
    database: 'Shop'